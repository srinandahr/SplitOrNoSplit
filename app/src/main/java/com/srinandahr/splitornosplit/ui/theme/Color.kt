package com.srinandahr.splitornosplit.ui.theme

import androidx.compose.ui.graphics.Color

// Brand colours, taken from the launcher icon: a royal-blue chat bubble on a gold field.
val BrandBlue = Color(0xFF1A56DB)
val BrandBlueDark = Color(0xFF102E86)
val BrandGold = Color(0xFFF5B301)
val BrandGoldDark = Color(0xFFB07F00)

// Semantic colours, carried over from the original XML layout so the app still
// looks like itself after the Compose migration.
val PositiveGreen = Color(0xFF1E8E3E)
val NegativeRed = Color(0xFFD93025)
val WarningOrange = Color(0xFFF57C00)

// Ledger direction. Green when money comes back to you, warm orange when it doesn't —
// the convention every split app uses, so it reads without a legend.
val LentGreen = Color(0xFF12805C)
val LentGreenDark = Color(0xFF4CD5A4)
val BorrowedOrange = Color(0xFFE0632B)
val BorrowedOrangeDark = Color(0xFFFF9366)

val SurfaceLight = Color(0xFFF8F9FA)
val CardLight = Color(0xFFFFFFFF)
val TextPrimaryLight = Color(0xFF202124)
val TextSecondaryLight = Color(0xFF5F6368)

val SurfaceDark = Color(0xFF121316)
val CardDark = Color(0xFF1E1F23)
val TextPrimaryDark = Color(0xFFE8EAED)
val TextSecondaryDark = Color(0xFF9AA0A6)
